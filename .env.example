# LLM Configuration
# OpenRouter Example:
# LLM_API_KEY=sk-or-v1-xxxxxxxx
# LLM_MODEL=google/gemini-2.0-flash-lite-preview-02-05:free
# LLM_BASE_URL=https://openrouter.ai/api/v1
# LLM_EMBEDDING_MODEL=text-embedding-3-small (Optional - requires provider support or fallback to OpenAI key)

# Default (OpenAI):
LLM_API_KEY=sk-your-openai-api-key
LLM_MODEL=gpt-4o-mini
# LLM_BASE_URL=https://api.openai.com/v1 (Default)
# LLM_EMBEDDING_MODEL=text-embedding-3-small (Default)

# ========================================
# Ingestion Sidecar (Python FastAPI)
# ========================================
# Run: cd backend/ingestion-service && start.bat  (Windows)
#       cd backend/ingestion-service && bash start.sh  (Linux/macOS)
INGESTION_SIDECAR_URL=http://localhost:8001

# Web Search (Serper API)
SERPER_API_KEY=your-serper-api-key

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3001/auth/google/callback

# Server
PORT=3001
FRONTEND_URL=http://localhost:5173
